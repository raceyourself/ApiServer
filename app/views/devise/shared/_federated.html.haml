.well
  - if devise_mapping.omniauthable?
    .btn-group
      - resource_class.omniauth_providers.each do |provider|
        = link_to provider.to_s.titleize, omniauth_authorize_path(resource_name, provider), class: 'btn btn-large'